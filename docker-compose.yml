services:
  api-gateway:
    build:
      context: ./services/api-gateway
      dockerfile: Dockerfile
    ports:
      - "8081:8080"
    environment:
      - PORT=8080
      - USERS_SERVICE_URL=https://users-service:8001
      - CONTENT_SERVICE_URL=https://content-service:8002
      - NOTIFICATIONS_SERVICE_URL=https://notifications-service:8005
      - SUBSCRIPTIONS_SERVICE_URL=https://subscriptions-service:8004
      - RATINGS_SERVICE_URL=https://ratings-service:8003
      - TLS_CERT_FILE=/app/certs/server.crt
      - TLS_KEY_FILE=/app/certs/server.key
    volumes:
      - ./certs:/app/certs:ro
    depends_on:
      - users-service
      - content-service
      - notifications-service
      - subscriptions-service
      - ratings-service
    networks:
      - music-streaming-network

  users-service:
    build:
      context: ./services/users-service
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    environment:
      - PORT=8001
      - JWT_SECRET=your-secret-key-change-in-production
      - MONGODB_URI=mongodb://mongodb-users:27017
      - MONGODB_DATABASE=users_db
      - TLS_CERT_FILE=/app/certs/server.crt
      - TLS_KEY_FILE=/app/certs/server.key
      # SMTP Configuration (set these to enable real email sending)
      # Option 1: Create .env file with your SMTP credentials (recommended)
      # Option 2: Replace the default values below directly
      # For Gmail: Create App Password at https://myaccount.google.com/apppasswords
      - SMTP_HOST=${SMTP_HOST:-smtp.gmail.com}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USERNAME=${SMTP_USERNAME:-your-email@gmail.com}
      - SMTP_PASSWORD=${SMTP_PASSWORD:-your-app-password-here}
      - SMTP_FROM=${SMTP_FROM:-your-email@gmail.com}
      - FRONTEND_URL=${FRONTEND_URL:-https://localhost:3000}
    volumes:
      - ./certs:/app/certs:ro
    depends_on:
      mongodb-users:
        condition: service_healthy
    networks:
      - music-streaming-network

  mongodb-users:
    image: mongo:7.0
    ports:
      - "27017:27017"
    volumes:
      - ./data/mongodb-users:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    networks:
      - music-streaming-network

  content-service:
    build:
      context: ./services/content-service
      dockerfile: Dockerfile
    ports:
      - "8002:8002"
    environment:
      - PORT=8002
      - MONGODB_URI=mongodb://mongodb-content:27017
      - MONGODB_DATABASE=music_streaming
      - JWT_SECRET=your-secret-key-change-in-production
      - SUBSCRIPTIONS_SERVICE_URL=https://subscriptions-service:8004
      - TLS_CERT_FILE=/app/certs/server.crt
      - TLS_KEY_FILE=/app/certs/server.key
    volumes:
      - ./certs:/app/certs:ro
    depends_on:
      mongodb-content:
        condition: service_healthy
    networks:
      - music-streaming-network

  mongodb-content:
    image: mongo:7.0
    ports:
      - "27018:27017"
    volumes:
      - ./data/mongodb-content:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    networks:
      - music-streaming-network

  mongodb-ratings:
    image: mongo:7.0
    ports:
      - "27019:27017"
    volumes:
      - ./data/mongodb-ratings:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    networks:
      - music-streaming-network

  ratings-service:
    build:
      context: ./services/ratings-service
      dockerfile: Dockerfile
    ports:
      - "8003:8003"
    environment:
      - PORT=8003
      - CONTENT_SERVICE_URL=https://content-service:8002
      - MONGODB_URI=mongodb://mongodb-ratings:27017
      - MONGODB_DATABASE=ratings_db
      - TLS_CERT_FILE=/app/certs/server.crt
      - TLS_KEY_FILE=/app/certs/server.key
    volumes:
      - ./certs:/app/certs:ro
    depends_on:
      content-service:
        condition: service_started
      mongodb-ratings:
        condition: service_healthy
    networks:
      - music-streaming-network

  mongodb-subscriptions:
    image: mongo:7.0
    ports:
      - "27020:27017"
    volumes:
      - ./data/mongodb-subscriptions:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    networks:
      - music-streaming-network

  subscriptions-service:
    build:
      context: ./services/subscriptions-service
      dockerfile: Dockerfile
    ports:
      - "8004:8004"
    environment:
      - PORT=8004
      - CONTENT_SERVICE_URL=https://content-service:8002
      - MONGODB_URI=mongodb://mongodb-subscriptions:27017
      - MONGODB_DATABASE=subscriptions_db
      - NOTIFICATIONS_SERVICE_URL=https://notifications-service:8005
      - TLS_CERT_FILE=/app/certs/server.crt
      - TLS_KEY_FILE=/app/certs/server.key
    volumes:
      - ./certs:/app/certs:ro
    depends_on:
      content-service:
        condition: service_started
      mongodb-subscriptions:
        condition: service_healthy
      notifications-service:
        condition: service_started
    networks:
      - music-streaming-network

  notifications-service:
    build:
      context: ./services/notifications-service
      dockerfile: Dockerfile
    ports:
      - "8005:8005"
    environment:
      - PORT=8005
      - CASSANDRA_HOSTS=cassandra:9042
      - CASSANDRA_KEYSPACE=notifications_keyspace
      - TLS_CERT_FILE=/app/certs/server.crt
      - TLS_KEY_FILE=/app/certs/server.key
    volumes:
      - ./certs:/app/certs:ro
    depends_on:
      - cassandra
    networks:
      - music-streaming-network

  cassandra:
    image: cassandra:4.1
    ports:
      - "9042:9042"
    volumes:
      - ./data/cassandra:/var/lib/cassandra
    networks:
      - music-streaming-network
    environment:
      - CASSANDRA_CLUSTER_NAME=MusicStreamingCluster
      - MAX_HEAP_SIZE=512M
      - HEAP_NEWSIZE=100M

  recommendation-service:
    build:
      context: ./services/recommendation-service
      dockerfile: Dockerfile
    ports:
      - "8006:8006"
    environment:
      - PORT=8006
      - TLS_CERT_FILE=/app/certs/server.crt
      - TLS_KEY_FILE=/app/certs/server.key
    volumes:
      - ./certs:/app/certs:ro
    networks:
      - music-streaming-network

  analytics-service:
    build:
      context: ./services/analytics-service
      dockerfile: Dockerfile
    ports:
      - "8007:8007"
    environment:
      - PORT=8007
      - TLS_CERT_FILE=/app/certs/server.crt
      - TLS_KEY_FILE=/app/certs/server.key
    volumes:
      - ./certs:/app/certs:ro
    networks:
      - music-streaming-network

networks:
  music-streaming-network:
    driver: bridge

# Volumes are now bind mounts in ./data/ directory
# Data is stored locally and can be shared via git